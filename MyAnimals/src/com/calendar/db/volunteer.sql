
CREATE TABLE VOLUNTEER(
MEMBER_ID VARCHAR2(400) REFERENCES MEMBER(MEMBER_ID) PRIMARY KEY,
VOLUNTEER_TITLE VARCHAR2(300) NOT NULL,
VOLUNTEER_CONTENT VARCHAR2(400) NOT NULL,
VOLUNTEER_ENDDATE DATE NOT NULL,
VOLUNTEER_STARTTIME VARCHAR2(300) NOT NULL,
VOLUNTEER_ENDTITME VARCHAR2(300) NOT NULL
);

DROP TABLE VOLUNTEER;


CREATE TABLE VOLUNTEER(
MEMBER_ID VARCHAR2(400) REFERENCES MEMBER(MEMBER_ID) PRIMARY KEY,
VOLUNTEER_TITLE VARCHAR2(300) NOT NULL,
VOLUNTEER_CONTENT VARCHAR2(1000) NOT NULL,
VOLUNTEER_MDATE VARCHAR2(12) NOT NULL,
VOLUNTEER_SEQ NUMBER NOT NULL,
VOLUNTEER_REGDATE DATE NOT NULL
);


insert into volunteer values ('ADMIN', '적당히해', '알겠냐?', '20190101', 1, sysdate);